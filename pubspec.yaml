name: coin_log
description: Coin Log.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 0.0.1+1

environment:
  sdk: ^3.6.0

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.2
  logging: ^1.3.0
  flutter_keyboard_visibility: ^5.4.1
  sqflite: ^2.3.0
  path: ^1.9.0
  fluttertoast: ^8.2.12
  reorderables: ^0.4.2
  math_expressions: ^2.7.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true